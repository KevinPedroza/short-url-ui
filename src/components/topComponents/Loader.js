import React from 'react';

const Loader = () => {
    return (
        <div className="ui active inverted dimmer">
            <div className="ui medium text loader">Loading URLS</div>
        </div>
    );
};

export default Loader;